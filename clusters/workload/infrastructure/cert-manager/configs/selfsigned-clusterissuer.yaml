# Bootstrap issuer: signs the self-signed root CA certificate below. Nothing else in
# the cluster should reference this issuer directly - workloads use the derived
# local-ca ClusterIssuer instead.
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: selfsigned
spec:
  selfSigned: {}
